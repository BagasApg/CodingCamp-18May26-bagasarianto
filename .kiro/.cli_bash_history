hi
make me a react starting project file
make an array of objects about courses with id, name, and number of exercises. put it in App.jsx, then display it with a component Display that is put in other module called components/Display.jsx
make a login page with glasmorphism style with html, tailwindcss, and js (for simple hover interaction)
Edit the index.html file so that the background is full page with cards ready, don't fill them yet. Put the stylings on the css/style.css. Prepare me a ready to work layouted page
/model
Make sure not to make test files for html and js, also to not include commands for terminal process
split the grid into 2 columns
Make the cards color base in white but still glassy. Add realtime timer with monospace font. Add Date (Day, month 1st, year). Add greetings with "John" as a name placeholder.
bring back my paddings, bring back the card for the header, make the cards a bit more glassier yet still white
remove the hover effect.
Remove one card so 3 left. Make grid allows right card to extend for listing todos. Fix title padding while center the first card of 1st column title. Rename in order from column 1 to 2 with Focus Timer, Quick Links, and Tasks respectively
The position for uncentered titles is still weird. Make the right card lengthen normally and only extend when needed
You lost your padding settings, put it back while applying my previous request
Add a button under the digital clock that says "too bright? ☀️" at light mode, "too dark? 🌙" at dark mode. Also add light/dark mode toggle switch with that text. no need to add button styling, just text
Add todo list function to the Tasks card. Add input form with submit button on the right with "Add". Save the data using local storage. Task contains checklist and the name, listed with delete button on the right.
Make the card color in light mode a bit whiter so the texts could be dark (black or grey) so they could be read more easily
too white, at least let the glass effect visible a bit
Add a timer of 25 minutes in the Focus Timer card with monospace font, followed with start button (colored button), stop and reset (white default button).
Make the countdown font in monospace. In Quick Links card, add text inputs for link name and URL with preset link shortcuts for google, gmail, and calendar. Add small delete button on each shortcuts
shortcuts are in button, not link. Make the add buttons for all page to be white with dark text
shortcuts in button, not pills. Also make it clickable as a button, not just the texts
Make the greeting based on the time, if it's morning, it says morning/
I want to add randomzied quotes in the header card from popular figures about routine, productivity, and dedication. Add at least 6 quotes. Put it wherever you feel its the best
I want to add randomzied quotes in the header card from popular figures about routine, productivity, and dedication. Add at least 6 quotes. Put it wherever you feel its the best
I want to add randomzied quotes in the header card from popular figures about routine, productivity, and dedication. Add at least 6 quotes. Put it wherever you feel its the best
I want to add randomzied quotes in the header card from popular figures about routine, productivity, and dedication. Add at least 6 quotes. Put it wherever you feel its the best
I want to add randomzied quotes in the header card from popular figures about routine, productivity, and dedication. Add at least 6 quotes. Put it wherever you feel its the best
Add a little pencil logo to replace the wave emoji. When clicked, make the text into a text-only input to change the name and store the submitted name into localstorage.
For the Tasks feature, add a rule that says no duplicate name. Give little warning under the input if duplicate detected
Make the inputs in the same color as the input so it can be read easier
Make the inputs in the same color as the submit button so it can be read easier (light mode)
change the pencil emoji into little text "(not your name?"). Darken the preview text in the input
put the not your name? under the greeting. Make the preview text (New task...), (Name)
revert the placeholder change, i meant to change it a bit darker
where is the .kiro folder?
